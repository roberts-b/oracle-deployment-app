import React from 'react'
import { Dropdown } from 'semantic-ui-react'

const BasicDropDown = () => (
  <Dropdown placeholder={placeholder} value={value} options={options} />
)

export default BasicDropDown;